/**
 * Host half of dsh-blue-whale.
 *
 * A no-op loader entry. The skin lives in the browser half (`./client`),
 * which dsh-client-modules loads from the package's `dsh.client` field.
 */
export function apply() {}
